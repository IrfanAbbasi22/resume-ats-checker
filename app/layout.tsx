import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'ATS Checker - Resume Analysis Tool',
  description: 'Analyze your resume against job descriptions and get improvement suggestions to pass ATS systems.',
  keywords: 'ATS, resume, job application, keyword matching, resume optimization',
  authors: [{ name: 'ATS Checker Team' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gray-50">
          {children}
        </div>
      </body>
    </html>
  )
} 